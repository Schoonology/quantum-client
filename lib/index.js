const bitsToBuffer = require('bits-to-buffer')
const deviceIdToAddress = require('device-id-to-address')
const sendBitsToAddress = require('send-bits-to-address')

function send(deviceId, bits) {
}

module.exports = {
  send
}
